// Entry point for the BUSY Bar tool.
//
// Nothing is wired up to the device yet -- this exists so that the build, type
// check, and lint pipelines have something to run against.
process.stdout.write("busybar: nothing wired up yet\n");
